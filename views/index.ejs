<!DOCTYPE html>
<html>
<head>
  <title>Random Dog Facts</title>
  <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
  <h1>Welcome to the Dog Facts App!</h1>
  <div class="facts">
    <% if (facts.length > 0) { %>
      <% facts.forEach(fact => { %>
        <p class="fact"><%= fact %></p>
      <% }) %>
    <% } else { %>
      <p>No dog facts available at the moment. Please try again later!</p>
    <% } %>
  </div>
</body>
</html>